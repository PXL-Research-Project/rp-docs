# LABS: Networks and web protocols

ping, ipconfig, traceroute, nslookup, dig
Inspecting network traffic using browser devtools, Postman and similar tools
Using curl
Running a minimal Python web server
Simple cloud deployment (for example GitHub Pages or S3)
Basic Git operations (kept very simple, optionally in teams), including merging and handling merge conflicts
